# reports directory

This folder is generated automatically during test runs.

| File | Generated by |
|------|-------------|
| `test-report.html` | `npm test` via jest-html-reporters |
| `coverage/` | `npm run test:coverage` via Jest built-in coverage |

Do not commit generated reports — they are excluded via `.gitignore`.
